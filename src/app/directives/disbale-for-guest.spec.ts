import { DisbaleForGuest } from './disbale-for-guest';

describe('DisbaleForGuest', () => {
  it('should create an instance', () => {
    const directive = new DisbaleForGuest();
    expect(directive).toBeTruthy();
  });
});
